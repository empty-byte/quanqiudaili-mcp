# quanqiudaili-mcp 联调记录

- 日期：2026-09-24
- 版本：0.1.0，SDK `@modelcontextprotocol/server` 2.1.0
- 原则：自动化测试不打线上；凡是会在后端创建数据的操作（新增子账号、下单、续费、带宽升级、退单）与删除操作，由维护者手动测试，不由自动化或 AI 代跑。

## 一、已自动验证（本地假后端，`npm test`）

| 项 | 结果 |
|---|---|
| 单元：表单编码 3 例、请求与响应映射 10 例、文档合并 15 例 | 通过 |
| 快照：81 个文档页生成 34 个工具、20 个只读；名称、必填集、拆分、括号参数结构、兜底修正 | 通过 |
| stdio 端到端：query 19 工具 + 1 资源、manage 33 工具、GET 带 query 与 token、POST 表单展开括号参数、schema 校验失败不打后端、未知工具抛协议错误、缺参数与缺 token 退出码 2 | 通过 |

## 二、待手动联调（需要测试账号 token）

配置方式见 README，默认模式就是全部工具，装一次即可。用 Inspector 手动点工具最方便，token 必须通过 `-e` 传入，终端里设的环境变量传不进子进程：

```
npx @modelcontextprotocol/inspector -e QQDL_TOKEN=你的token -- node <安装目录>/dist/src/index.js
```

### 只读工具（20 个，无任何副作用）

| 工具 | 建议提问 | 结果 | 备注 |
|---|---|---|---|
| user_info | 查一下我的账号信息 | | |
| user_balance | 我还有多少余额 | | 应只返回金额 |
| product_unit_price | 各产品现在的单价 | | |
| product_list | 有哪些产品 | | |
| dynamic_country_list | 动态住宅不限时长有哪些国家 | | |
| dynamic_state_list | 美国动态住宅有哪些州 | | |
| dynamic_city_list | 美国动态住宅有哪些城市 | | |
| static_country_list | 静态住宅普通产品能买哪些国家 | | |
| static_city_list | 美国静态住宅普通有哪些城市 | | |
| static_region_stock | 看看美国静态住宅普通的库存 | | |
| static_ip_range_list | 美国静态住宅普通有哪些 IP 段 | | |
| stock_check | 帮我检测美国普通静态住宅 30 天 2 个 IP 有没有库存 | | 不下单 |
| sub_account_list | 列出动态住宅子账号第一页 10 条 | | |
| sub_account_flow_query | 子账号 X 本月用了多少流量 | | |
| sub_account_limit_flow | 子账号 X 的流量上限是多少 | | |
| bandwidth_package_list | 静态住宅普通有哪些带宽套餐 | | |
| bandwidth_monitoring_list | 子账号 X 的带宽监控 | | |
| bandwidth_trend | 子账号 X 最近的带宽趋势 | | |
| bandwidth_detail | 子账号 X 有效期内的带宽详情 | | |
| bandwidth_upgrade_price | 给子账号 X 升到 30M 带宽多少钱 | | 不下单 |

### 写操作确认（每个客户端各验一次）

| 客户端 | 预期 | 结果 |
|---|---|---|
| Inspector | 调 sub_account_update 时弹出确认表单，内容含工具名与参数；拒绝则返回"弹窗没有得到确认"的待确认文本和 confirm_token，后端无请求 | |
| Claude Code | 同上；若不弹窗而是返回"待确认"文本和 confirm_token，说明该版本不支持 elicitation 或声明了却不显示弹窗（2026-09-25 VS Code 插件实测：声明支持，但不显示弹窗就直接回 decline），走的是确认码路径 | 2026-09-25 VS Code 插件：通过。order_renew 第一次返回预览与确认码，用户在对话里同意后带码重调，续费成功 |
| Codex CLI / Cursor / VS Code / Zed | 同上，记录各自走的是弹窗还是确认码 | |
| 任一客户端加 `--yes` | 直接执行，不确认 | |

### 无资金影响、可改回的写操作

| 工具 | 步骤 | 结果 | 备注 |
|---|---|---|---|
| sub_account_update | 给测试子账号改备注再改回 | | 确认 POST 表单能被后端解析 |

### 创建与删除类操作（由维护者决定是否执行）

| 工具 | 影响 | 结果 |
|---|---|---|
| sub_account_add | 创建子账号 | |
| sub_account_set_credentials / sub_account_toggle_direct / sub_account_set_limit_flow / sub_account_update_batch | 修改子账号配置 | |
| order_buy_dynamic / order_buy_time_ip / order_buy_test_ip / order_renew / order_bandwidth_upgrade | 从余额扣费 | order_renew 2026-09-25 通过：静态原生子账号 77 续 7 天不含带宽，扣 0.01 元，到期 2026-10-17 → 2026-10-24，返回订单号与新到期时间，但把该子账号原本开着的自动续费关掉了（后端把没传的 is_renew 当 0 写回），已在 src/renew.ts 兜底：续费前查当前设置原样带上，待再次手测；其余待测 |
| order_refund_apply | 申请退单 | |
| sub_account_delete / sub_account_delete_batch | 不可恢复 | |

### token 失效路径

把 `QQDL_TOKEN` 改成任意错误值重启后调用 `user_info`，应得到"token 无效（请先登录）…API Keys…"的错误文本。结果：

## 三、发现的文档缺陷与已加的兜底

见设计文档第 11 节缺陷表；对应条目都在 `spec/overrides.yaml` 里带 "文档缺陷兜底" 注释，文档站修好后删除。

## 四、遗留问题

- 需要一个测试账号 token 才能完成第二部分。
- 已核对后端源码：`portEnableUpdateList` 只接受产品 2、3、4，`sub_account_toggle_direct` 的枚举无误；`common/stateList`、`cityList`、`countryList` 接受产品 1、6，对应工具枚举已补齐。
