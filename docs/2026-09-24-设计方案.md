# quanqiudaili-mcp 设计方案

- 日期：2026-09-24
- 状态：已实现并提交，随代码持续修订（最近一次 2026-09-25）；实施过程与后续修订清单见 `2026-09-24-实施计划.md` 的"实施后修订"，联调结果见 `2026-09-24-联调记录.md`
- 范围：全球代理对外 API（`externalapi`）的 MCP 适配层
- 上游文档：<https://develop.quanqiudaili.com/llms.txt>（Apifox 导出，每页内嵌 OpenAPI 3.0.1 YAML）
- 后端仓库：`proxy-distribution-system`，模块 `application/externalapi`

## 1. 背景与目标

把已经对外开放的 `externalapi` 接口以 MCP 形式提供给 AI 客户端（Claude Code、Claude Desktop 等），让模型能够查询子账号、库存、价格，并在用户确认后执行下单、续费、改配置等操作。

MCP 服务器只是一个 HTTP 客户端：业务逻辑、参数校验、鉴权、扣费全部仍在 PHP 后端执行，本项目不改后端任何代码。

### 已确认的决策

| 项 | 决定 |
|---|---|
| 项目形态 | 独立仓库，TypeScript，官方 MCP TypeScript SDK v2（`@modelcontextprotocol/server` 2.1），Node ≥ 20。面向其他使用者，环境要求与安装示例不以开发者本机为准 |
| 传输 | stdio 本地运行；远程 Streamable HTTP 不在本期 |
| 拆分 | 一个启动命令：默认全部工具，`--readonly` 只暴露只读工具，同一份工具定义按 readOnly 过滤。2026-09-24 从"两个 server"改为单命令，使用者只装一次；`setup` 子命令打印各客户端配置片段，不写文件 |
| 鉴权 | 仅 token 模式，token 是网站 API Keys 页面生成的 API Key，环境变量传入 |
| 购买接口 | `createProductBuyOrder` 拆成动态流量、时长 IP 两个 tool |
| HTTP 动词 | 查询类 GET，操作类一律 POST（后端已确认不校验动词） |
| 参数类型 | `product_type_id` 统一为整数 |
| 分发 | git clone 后本地构建，不发布 npm |
| 工具定义来源 | 从文档站 OpenAPI 生成，不手写 |

### 非目标

- 不覆盖前台 `api` 模块的接口。
- 不做 OAuth、多用户远程服务、token 自动刷新。
- 不在 MCP 层重做参数校验与业务规则，后端返回什么错误就透传什么。

## 2. 总体架构

```
Apifox 文档站 ──(开发期: npm run sync-docs)──▶ spec/pages/**/*.md + spec/overrides.yaml ──▶ spec/tools.json
                                                                                              │
                                                                                    (运行期: 只读本地文件)
                                                                                              ▼
AI 客户端 ◀── stdio ──▶ quanqiudaili-mcp [--readonly] ── HTTPS ──▶ https://admin.quanqiudaili.com/externalapi/*
                                                                         header: token
```

- 生成是开发期动作，输出提交进仓库；运行期不联网拉文档，启动即用。
- 默认注册全部工具，`--readonly` 按工具的只读标注过滤；旧参数 `query`、`manage` 仍接受。

### 技术选型

| 用途 | 选择 | 说明 |
|---|---|---|
| MCP 协议 | `@modelcontextprotocol/server` 2.1（SDK v2） | 官方 SDK，stdio 入口 `serveStdio`，兼容 2025 与 2026-07-28 两代客户端；测试用 `@modelcontextprotocol/client` |
| 参数校验 | SDK 内置：`fromJsonSchema()` + Ajv | 生成好的 JSON Schema 原样注册，`tools/list` 与文档一致；不引入 zod，不做二次转换 |
| 表单编码 | 自写 `toForm()`，基于 `URLSearchParams` | 约 10 行产出 PHP 括号格式，不加 qs |
| YAML 解析 | `yaml` | 仅 sync 脚本使用 |
| 测试 | vitest | 单元测试、以真实文档页为输入的快照测试、起本地假后端的 stdio 端到端测试 |

### 目录结构

```
quanqiudaili-mcp/
  docs/                          设计方案、实施计划、联调记录
  spec/pages/<分组>/<控制器>-<方法>.md  sync-docs 下载的原始接口页，按文档站分组建目录，提交入库，是快照测试输入与 diff 基线
  spec/dynamic-proxy-session.md  Session 说明页副本，作为资源正文
  spec/overrides.yaml            人工维护：工具名、描述、标注、拆分、文档修正
  spec/tools.json                生成物，运行期唯一输入，提交入库
  scripts/sync-docs.ts           拉取 llms.txt 与全部接口页 → spec/pages/，再调 build-spec
  scripts/build-spec.ts          spec/pages + overrides → spec/tools.json，打印与上一版的差异
  scripts/lib/merge.ts           合并规则的纯函数
  src/index.ts                   入口：node dist/src/index.js [--readonly]，以及 setup 子命令
  src/setup.ts                   生成各客户端配置片段的纯文本函数
  src/http.ts                    请求封装：token、表单编码、响应映射
  src/form.ts                    表单编码
  src/types.ts                   ToolDef 类型
  test/                          单元、快照、stdio 端到端测试
  README.md                      安装与配置
```

## 3. 文档到工具的生成规则

`scripts/sync-docs.ts` 的处理步骤：

1. 抓取 `llms.txt`，下载全部 `*e0.md` 接口页，提取 ```yaml 块并解析。说明页（`*m0.md`）与示例数据模型（`*d0.md`）跳过，Session 说明单独处理见第 8 节。落盘位置按页面 `tags` 里的文档站分组，经 `TAG_DIR` 映射成英文目录名，六个产品分组带 `product_type_id` 前缀（如 `2-static-standard/`），其余四个是 `token/`、`user/`、`tool/`、`bandwidth/`，未登记的分组报错；文件名取接口路径 `控制器-方法.md`；同一分组同一接口有多页时（数据中心续费在文档站上有两页）文件名带上 Apifox 页面 id 区分（`placePages()`）。
2. **按 `path` 合并**，忽略文档中的动词。同一路径在不同产品目录下各有一页，参数集不同。
3. **参数取并集**：query 参数与表单 body 参数合成一张表。必填规则：在所有变体中都必填才标必填；否则可选，并在描述中注明"对某类产品必填"。
4. **`product_type_id` 归一**：类型整数，枚举取该路径所有变体页的固定值，描述附中文标签：1 动态住宅流量不限时长、6 动态住宅流量包月、2 静态住宅普通、3 静态住宅原生、4 静态住宅运营商原生、8 数据中心。
5. **括号参数转结构**：
   - `content[0][id]`、`content[0][customUsername]`、`content[0][customPassword]` → `content: object[]`
   - `appoint_ip[0][ip_str]`、`appoint_ip[0][count]` → `appoint_ip: object[]`
   - `ids[]`、`subAccounts[]`、`sub_accounts[]`、`subAccountIds[]` → 数组
   - `searchArr[0]`、`searchArr[1]` → `searchArr: string[]`
6. **动词**：工具标注为只读则 GET，否则 POST。
7. **overrides.yaml** 按路径覆盖：工具名、中文描述、只读与破坏性标注、归属 server、参数描述补充、拆分规则、忽略页。文档缺陷的兜底修正也放这里，文档修好后删除对应条目。
8. 输出 `spec/tools.json`（MCP 工具形态：名称、标题、描述、JSON Schema、标注、路径、动词、固定参数），原始页面保留在 `spec/pages/`。脚本结束时打印与上一版的差异，供人工审阅后提交。

## 4. 工具清单

共 34 个。名称为业务向 snake_case，不照搬控制器方法名。其中 user_balance 是派生工具：overrides 顶层 `derived` 从 user_info 复制并指定 `pick: money`，运行期只返回响应 data 里的这个字段。

### 只读工具，20 个（`--readonly` 模式暴露的全部）

| tool | 接口 | 说明 |
|---|---|---|
| user_info | GET user/getUserInfo | 账号信息与余额 |
| user_balance | GET user/getUserInfo | 账户余额，派生自 user_info，data 只取 money |
| product_unit_price | GET product_price/getProductUnitPrice | 各产品单价、折扣、按国家自定义价 |
| product_list | GET conpons/getProductList | 产品信息 |
| dynamic_country_list | GET common/countryList | 动态产品 1、6 可选国家 |
| dynamic_state_list | GET common/stateList | 动态产品州或省 |
| dynamic_city_list | GET common/cityList | 动态产品城市 |
| static_country_list | GET inventory/countryList | 时长产品 2、3、4、8 可选国家 |
| static_city_list | GET inventory/cityList | 时长产品城市 |
| static_region_stock | GET inventory/getBuyProductSku | 时长产品地区库存 |
| static_ip_range_list | GET inventory/ipStrList | 时长产品 IP 段 |
| stock_check | GET product_order/checkInventory | 下单前库存检测，不扣费 |
| sub_account_list | GET device/accountList | 子账号列表，覆盖全部产品 |
| sub_account_flow_query | GET device/subAccountFlowQuery | 动态子账号月度流量统计 |
| sub_account_limit_flow | GET tool/accountLimitFlow | 子账号流量上限与已用量 |
| bandwidth_package_list | GET bandwidth/getBandwidthList | 带宽套餐 |
| bandwidth_monitoring_list | GET bandwidth/monitoringList | 带宽监控 |
| bandwidth_trend | GET bandwidth/monitoringTrend | 子账号带宽趋势 |
| bandwidth_detail | GET bandwidth/bandwidthNumDetail | 有效期内带宽详情 |
| bandwidth_upgrade_price | GET product_price/bandwidthUpgradePrice | 升级价格试算 |

`stock_check` 与 `bandwidth_upgrade_price` 在文档中是 POST，但只是计算不写库，按"查询类 GET"规则处理；若联调发现后端只从 POST 读参，则在 overrides 中单独指定 POST。

### 写操作工具，14 个（默认模式额外暴露）

| tool | 接口 | 标注 |
|---|---|---|
| sub_account_add | POST device/accountAdd | 写，动态产品新增子账号 |
| sub_account_update | POST device/accountUpdate | 写，改备注 |
| sub_account_update_batch | POST device/accountUpdateList | 写 |
| sub_account_set_credentials | POST device/batchUpdateSubAccountUsernamePassword | 写，自定义账密 |
| sub_account_toggle_direct | POST device/portEnableUpdateList | 写，批量开关直连 |
| sub_account_set_limit_flow | POST tool/accountLimitFlowEdit | 写，设置流量上限 |
| sub_account_delete | POST device/accountDelete | 破坏性 |
| sub_account_delete_batch | POST device/accountDeleteList | 破坏性 |
| order_buy_dynamic | POST product_order/createProductBuyOrder，产品 1、6 | 扣费 |
| order_buy_time_ip | POST product_order/createProductBuyOrder，产品 2、3、4、8 | 扣费 |
| order_buy_test_ip | POST product_order/createTestOrder | 扣费 |
| order_renew | POST product_order/createRenewProductBuyOrder | 扣费 |
| order_bandwidth_upgrade | POST product_order/createBandwidthUpgradeOrder | 扣费 |
| order_refund_apply | POST order_back/createOrderBack | 写，申请退单 |

默认模式同时暴露 20 个只读工具与 14 个写操作，共 34 个，"先查后改"在一个 server 里完成。

登录、登出、token 检测与刷新接口已随 API Key 改造从文档站下线（2026-09-25 同步确认），overrides 的 ignore 保留这四个路径，即使恢复也不暴露。

### 参数要点

- 分页工具描述注明 `pagesize` 最大 100。
- 子账号 id 类参数一律 string，数组元素也是：后端经表单接收本来就是字符串，统一后模型和人都不会因 77 与 "77" 之差被校验拒绝；`subAccountStart`/`subAccountEnd` 是数值区间、`order_product_buy_id` 是订单号，保持 integer。
- 购买与续费的 `num` 范围 1 到 300，来自后端校验规则。
- `order_buy_dynamic` 参数：`product_type_id`（1 或 6）、`num`、`bill_timelen`（仅 6 有效）。
- `order_buy_time_ip` 参数：`product_type_id`（2、3、4、8）、`num`、`timelen`、`agree`、`country`、`city`、`appoint_ip`、`exclude_ip_str`、`bandwidth_num`、`remark`、`use_ip_port`、`random_account_password`。
- `country` 一律为 ISO 3166-1 二字码，如 `US`，在描述中说明。

## 5. 鉴权与配置

| 环境变量 | 必填 | 默认值 | 说明 |
|---|---|---|---|
| `QQDL_TOKEN` | 是 | 无 | 网站 API Keys 页面生成的 API Key，永久有效，每账号最多 20 个 |
| `QQDL_BASE_URL` | 否 | `https://admin.quanqiudaili.com` | 便于指向测试环境 |
| `QQDL_TIMEOUT_MS` | 否 | `30000` | 单次请求超时 |
| `QQDL_CONFIRM_QUIET_MS` | 否 | `10000` | 确认码发出后的静默期，见第 7 节 |

- token 放请求头 `token`。
- 每个请求都附带参数 `is_mcp_send=1`（GET 在查询串、POST 在表单）和请求头 `User-Agent: quanqiudaili mcp`，后端据此区分 MCP 发出的请求（2026-09-25 加）。
- 收到 HTTP 401 或后端返回"请先登录"类错误时，工具返回明确提示：token 无效，请到 API Keys 页面核对或重新生成并更新配置。
- token 不写日志、不落盘。stdio 模式下 stdout 是协议通道，所有日志只走 stderr。

## 6. 请求与响应映射

- GET：参数进 query string。
- POST：`Content-Type: application/x-www-form-urlencoded`，用自写的 `toForm()` 生成 `content[0][id]=...` 这类 PHP 括号格式。
- 参数类型由 SDK 按 JSON Schema 在调用前校验，不符合时直接返回 `isError` 的校验提示，不请求后端。
- 响应统一为 `{code, msg, time, data}`：
  - `code === 1`：返回 `{code, msg, data}` 的 JSON 文本，`code` 归一为数字。不附 `structuredContent`。
  - 其它 `code`：`isError: true`，文本为 `msg`。
  - 非 JSON 或 HTTP 5xx：`isError: true`，带状态码与前 200 字符正文。
  - 后端限流"访问频繁"原样透传，并注明限额为每 IP 每分钟 180 次。

## 7. 安全与副作用标注

- 只读工具：`readOnlyHint: true`。
- 删除与扣费工具：`destructiveHint: true`，`idempotentHint: false`；描述首句写明"会从账户余额扣费"或"不可恢复"，让客户端在调用前弹出确认。
- 写操作确认（`src/confirm.ts`）：14 个非只读工具的处理器由 `withConfirm` 包装。客户端声明了 elicitation 能力（2025 版协议看初始化时的能力，2026 版看请求信封里的 `CLIENT_CAPABILITIES_META_KEY`）就返回 `inputRequired`，客户端弹出预览表单，用户 accept 且 confirm=true 才调后端；decline、cancel、未勾选都不当作最终拒绝，而是退到确认码并在返回文本里说明弹窗没得到确认，由模型在对话里再问用户一次。原因是 Claude Code 的 VS Code 插件声明支持 elicitation 却不显示弹窗就直接回 decline（2026-09-25 实测），把它当取消会让写操作在这类客户端永远做不成；退到确认码后仍要用户在对话里明确同意才执行，安全性不变，代价只是真弹窗客户端上用户拒绝后会被再问一次。不支持 elicitation 时同样退到一次性 `confirm_token`（工具名加参数的 SHA-256，5 分钟有效，用一次作废，参数变了作废，发出后有静默期，默认 10 秒，`QQDL_CONFIRM_QUIET_MS` 可调：静默期内带码调用返回"还不能用"、不消费、生效时间再顺延一个静默期，挡的是模型拿到码不问用户就重调的情况，2026-09-25 在不支持弹窗的客户端上实测出现过第二次同样的续费没问用户就执行；用户看完预览再回复通常超过静默期，不受影响），第一次调用只返回预览和确认码；带确认码的调用直接校验，不再弹窗。预览由工具标题、描述首句、逐个参数拼成，`product_type_id` 带中文产品名，不查价不查余额。`--yes` 关闭确认。写工具的 schema 注册时多一个可选 `confirm_token`，`tools.json` 不含。
- 默认模式启动时在 stderr 打印一行提示，说明含扣费与删除操作且执行前会确认，只查询请加 `--readonly`。
- 未在文档中公开的接口一律不暴露，例如 `accountDeleteAll`、`accountSwitch`、`account_verify`、`account_info`。

## 8. 资源

文档站的三篇说明页处理如下，已确认。

| 页面 | 处理 | 理由 |
|---|---|---|
| 动态IP Session验证（不限时长、包月两页，内容完全相同） | 做成一个资源 `quanqiudaili://docs/dynamic-proxy-session`，两个 server 都提供，随 sync 更新 | 这是用法知识而非操作：账号后缀参数 `cty`、`st`、`ct`、`ss`、`tm`、`spec` 的规则、正确与错误示例、资源池性能表。模型回答"怎么拿一个美国 10 分钟不变的 IP"时需要它 |
| 查询国家编码 | 不做资源 | `dynamic_country_list` 与 `static_country_list` 返回的是各产品实际可购买的国家与编码，比静态表准确；模型本身熟悉 ISO 二字码。只在工具描述里注明格式 |

不做本地拼接连接串的工具：连接串写法只以上述资源形式提供，由模型阅读后自行拼接。

## 9. 测试

- 快照测试（`test/spec.test.ts`，11 例）：以入库的 81 个文档页为输入运行合并逻辑，断言 34 个工具、20 个只读，名称、必填集、拆分、括号参数结构、兜底修正、子账号 id 类型、页面目录布局与 `tools.json` 一致，外加整体快照。
- 单元测试：表单编码 3 例、请求与响应映射 17 例、文档合并与派生 21 例、确认预览与确认码 8 例、续费兜底 6 例、配置片段 6 例。
- 端到端（`test/server.test.ts`，17 例）：起本地假后端，用真实 stdio 连接验证工具数量与标注、GET/POST 编码、schema 拦截、退出码、弹窗确认（2025 与 2026 两版协议）、确认码与静默期、`--yes`、续费兜底、派生工具。
- 合计 8 个文件 89 例（2026-09-25），`npm test` 不联网。
- 联调：用测试账号把 20 个只读工具各跑一次；写操作只跑无资金影响的 `sub_account_update` 改备注。新建、扣费、删除类工具由维护者手动测试，不进自动化。

## 10. 安装与使用

要求 Node.js 20 或更新，Windows、macOS、Linux 均可。token 是网站 API Keys 页面生成的 API Key。接入客户端两种方式二选一，细节与各客户端片段以 README 为准：

- 方式一，npx 从 GitHub 拉起，不下载不安装：客户端配置里命令写 `npx -y github:empty-byte/quanqiudaili-mcp`，第一次拉起时 npm 克隆仓库、装依赖、跑 `prepare` 编译并缓存，本机要有 `git`。`npx -y github:empty-byte/quanqiudaili-mcp setup --npx --token 你的token` 打印全部客户端的配置片段。
- 方式二，克隆到本机：`git clone https://github.com/empty-byte/quanqiudaili-mcp.git && npm ci`，`npm ci` 结束时自动编译到 `dist/`，启动命令 `node <安装目录>/dist/src/index.js`；`setup --token 你的token` 打印配置片段。

两种方式的启动参数相同：不带参数全部工具，`--readonly` 只读，`--yes` 跳过写操作确认。npm 上尚未发布，注册好后 `npm publish`，`setup --npx` 的包名在 `src/setup.ts` 的 `PACKAGE` 一处切换。

## 11. 文档缺陷跟踪

| 缺陷 | 状态 | 生成器兜底 |
|---|---|---|
| 静态住宅普通子账号列表页路径写成 `/api/device/accountList` | 已修，线上确认 | 无需 |
| 索引描述与标题不符 | 大部分已修，"刷新Token"一条仍是"检测Token是否过期" | 生成只用页内 summary，不用索引描述 |
| 动词混用 | 规则已定：查询 GET、操作 POST | 按只读标注决定动词 |
| `product_type_id` 类型 string 与 integer 混用 | 规则已定：整数 | 强制整数 |
| `accountLimitFlowEdit` 旧页缺 `type`、`timelen`，`limit_flow` 非必填 | 待修 | 合并时以新页为准 |
| `accountLimitFlowEdit` 新页 `timelen` 为 string，`type` 描述仍写"0=不限时长" | 待修 | overrides 覆盖类型与描述：0 = 永久，按累计用量 |
| `accountLimitFlow` 两页 `id` 非必填、无描述 | 待修 | overrides 置必填并补描述 |
| 动态住宅（包月）国家列表页把 `product_type_id` 的固定值也写成 1 | 待修 | overrides 指定 enum [1, 6] |
| `ipStrList` 有一页把 `page` 类型写成 array | 待修 | 合并时标量优先并丢弃 items，overrides 指定 integer |
| `getBuyProductSku` 只有产品 2 那页把 `country_code` 标为必填，后端全要求 | 待修 | overrides 置必填 |
| 续费接口 `createRenewProductBuyOrder` 文档没写 `is_renew`，后端不传当 0 并连同 `renew_with_bandwidth` 写回子账号，经 API 续费会关掉自动续费（2026-09-25 实测） | 待修：文档补参数，或后端改为不传就不改设置 | 不在生成器：`src/renew.ts` 续费前查当前设置原样带上，工具不暴露 `is_renew` |
| `accountUpdateList` 的 `remark` 描述写成 "test" | 待修 | overrides 补描述 |
| 参数描述里夹着 `<a href>` 链接标签 | 文档站特性 | 生成时去掉 HTML 标签 |
| `common/stateList` 只有不限时长一页，后端对产品 1、6 都放行 | 文档缺页 | overrides 指定 enum [1, 6] |
| `createOrderBack` 的 `remark` 文档标为可选，后端 require | 已修，2026-09-25 同步确认三页均标必填 | overrides 只保留描述 |
| `createTestOrder` 的 `num` 文档写 1-20，后端只放行 1-10 | 待修 | overrides 改描述 |
| `accountList` 的 `search_type` 文档写"精准匹配"，后端按备注搜索是模糊匹配 | 待修 | overrides 改描述并加枚举 |
| `createProductBuyOrder` 时长产品页的 `timelen` 多写了 "5=3天"，后端只放行 0-4 | 待修 | overrides 加 enum [0-4] 并改描述 |
| `getBandwidthList` 的 `country` 文档标为可选，后端 require | 待修 | overrides 置必填并补描述 |

## 12. 实施步骤与估算

| 步骤 | 内容 | 估算 |
|---|---|---|
| 1 | 初始化项目、依赖、tsconfig、两个入口骨架 | 0.5 天 |
| 2 | 合并规则、sync-docs 与 build-spec 脚本、overrides，产出首版 tools.json，快照测试 | 1 天 |
| 3 | 运行时：工具注册、表单编码、响应映射、资源 | 0.5 天 |
| 4 | 工具描述润色、测试账号联调、README | 1 天 |

合计约 3 人日。

## 13. 开放问题与后续

- token 到期后的提醒方式，目前依赖调用失败时的错误提示。
- 后续可选：前台 `api` 模块接入、Streamable HTTP 远程模式、OAuth。
